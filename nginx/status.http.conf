# First install: issue the certificate before switching to status.conf.
server {
    listen 80;
    server_name status.sunrisevacation.cn;
    location /.well-known/acme-challenge/ { root /var/www/acme; }
    location / { return 503; }
}
