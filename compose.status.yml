# Include on every future Nginx recreation after installation.
services:
  nginx:
    volumes:
      - ./status:/usr/share/nginx/status:ro
      - ./nginx/status-site.conf:/etc/nginx/conf.d/status.conf:ro
